# 🎵 Spotify UI (React + JavaScript)

A simple front-end clone 
Built with React and plain CSS (no Tailwind) to practice component structure, routing, and modern UI layout.

## 🚀 Features
- React + Vite setup
- Reusable components (`Header`, `PlaylistCard`, etc.)
- Home screen displaying playlists
- CSS variables for Spotify’s dark theme
- Ready for additional pages: Search, Library, Playlist

## 🧰 Tech Stack
- **React** (Vite)
- **JavaScript (ES6+)**
- **CSS Modules / Plain CSS**
- **React Router DOM**

## 🖥️ Run Locally
```bash
git clone https://github.com/DarilynP/spotify.git
cd spotify
npm install
npm run dev
